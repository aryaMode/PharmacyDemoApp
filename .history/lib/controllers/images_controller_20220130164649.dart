import 'package:firebase_database/firebase_database.dart';

class ImageController {
  <DatabaseReference?>? FutureBuilder(
    future: Future,
    initialData: InitialData,
    builder: (BuildContext context, AsyncSnapshot snapshot) {
      return ;
    },
  ),
}
